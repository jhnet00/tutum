apiVersion: apps/v1
kind: Deployment
metadata:
  name: news-consumer
  namespace: tutum-app
  labels:
    app: news-consumer
    app.kubernetes.io/name: news-consumer
    app.kubernetes.io/part-of: tutum
spec:
  replicas: 1
  selector:
    matchLabels:
      app: news-consumer
  template:
    metadata:
      labels:
        app: news-consumer
    spec:
      nodeSelector:
        workload: app
      imagePullSecrets:
        - name: harbor-secret
        - name: gitlab-registry-secret
      containers:
        - name: news-consumer
          image: registry.gitlab.com/tutum-project/tutum-app/backend/workers:latest
          imagePullPolicy: Always
          command: ["python", "-u", "consumer_news.py"]
          envFrom:
            - configMapRef:
                name: news-pipeline-config
            - secretRef:
                name: news-pipeline-secret
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
